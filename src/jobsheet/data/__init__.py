"""Static reference tables that are data, not logic.

Nothing here reaches the network or the disk. A module in this package is a
list somebody would otherwise have to type from memory, checked in so that
JobSheet keeps working with the cable pulled out.
"""
